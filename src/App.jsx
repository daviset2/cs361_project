import WeatherPage from "./pages/WeatherPage"
export default function App() {
    return (
        <WeatherPage /> /* I guess since theres only one page I could have written it here, but why do that when this works just as well */
    )
}
